# الويب — مشروع أيهم

هذا المجلد يحتوي على الواجهة الويب لمشروع **أيهم — العقل الرقمي الحي**.

## محتويات المجلد
- `index.html`  
  واجهة التفاعل الأساسية (اسأل أيهم / تحليل)

- `ayhem-style.css`  
  الهوية البصرية والستايل العام

- `ayhem-core.js`  
  منطق التفاعل والتحليل الأولي

- `ayhem-ai.js` (لاحقًا)  
  ربط الواجهة بمحرك الذكاء الاصطناعي

## الحالة الحالية
🟢 تم إنشاء الهيكل  
🟡 الواجهة تعمل محليًا  
🔴 الربط بالذكاء الاصطناعي قيد التطوير

## الهدف
تحويل أيهم إلى واجهة ويب حيّة تمثل **عقلًا رقميًا مستقلًا** قابلًا للتوسع.<!DOCTYPE html>
<html lang="ar">
<head>
  <meta charset="UTF-8">
  <title>أيهم — العقل الرقمي الحي</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="ayhem-style.css">
</head>
<body>

  <div class="container">
    <h1>أيهم</h1>
    <p class="subtitle">أول عقل رقمي حي — واجهة التفاعل</p>

    <div class="ask-box">
      <input
        type="text"
        id="questionInput"
        placeholder="اسأل أيهم..."
        autocomplete="off"
      />
      <button onclick="analyze()">تحليل</button>
    </div>

    <div id="output" class="output">
      <!-- نتيجة التحليل تظهر هنا -->
    </div>
  </div>

  <script src="ayhem-core.js"></script>
</body>
</html>

 body {
  margin: 0;
  padding: 0;
  font-family: "Tahoma", Arial, sans-serif;
  background: #0b0f1a;
  color: #e6e6e6;
}

.container {
  max-width: 600px;
  margin: 60px auto;
  padding: 20px;
  text-align: center;
}

h1 {
  font-size: 42px;
  margin-bottom: 5px;
  color: #4da6ff;
}

.subtitle {
  font-size: 14px;
  color: #9aa4b2;
  margin-bottom: 40px;
}

.ask-box {
  display: flex;
  gap: 10px;
}

.ask-box input {
  flex: 1;
  padding: 12px;
  font-size: 16px;
  border-radius: 6px;
  border: none;
  outline: none;
}

.ask-box button {
  padding: 12px 18px;
  font-size: 16px;
  border: none;
  border-radius: 6px;
  background: #4da6ff;
  color: #000;
  cursor: pointer;
}

.ask-box button:hover {
  background: #3399ff;
}

.output {
  margin-top: 30px;
  padding: 15px;
  background: #11162a;
  border-radius: 6px;
  text-align: right;
  min-height: 60px;
  font-size: 15px;
  line-height: 1.6;
}function analyze() {
  const input = document.getElementById("questionInput");
  const output = document.getElementById("output");

  const question = input.value.trim();

  if (question === "") {
    output.innerHTML = "❗ الرجاء كتابة سؤال.";
    return;
  }

  // منطق أولي (رمزي)
  output.innerHTML =
    "🧠 أيهم يستقبل سؤالك...<br><br>" +
    "<strong>السؤال:</strong> " + question + "<br><br>" +
    "🔍 التحليل قيد التطوير. هذه الواجهة هي نقطة البداية.";

  input.value = "";
}
